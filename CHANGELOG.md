# 更新说明

## 2026-09-10 — 动态采样预览与可选分块

合并上游截至 `5fede41277afea249ca4c4f2775293ff58de45f2` 的 3 个提交。

- 采样预览改为最多 16 帧的循环 WebP，移除生成结束后的整段 JPEG 回放，减少整段预览编码和传输数据。成片从下游 CreateVideo / SaveVideo 查看；动态预览比单帧预览仍有额外开销，可关闭实时预览。
- H3 latent 放大支持可选时间分块，默认关闭。较长 latent 按时间窗口处理并混合重叠区域；旨在降低中间激活峰值，但不保证与整段计算逐像素一致。
- Refine 二采支持可选空间分块，默认关闭。沿空间长轴分块计算、混合预测，音频保持完整；分块可能影响画面一致性和耗时，不承诺速度提升。建议先保持默认设置，需要时再对比开启效果。
- 冲突整合保留本分支模型缓存清理、内存诊断、采样计时和低内存导出保护，以及上一版「保完整」句尾防裁切逻辑。
- 补充分块条件布局构建失败时的完整回滚，防止退回整帧采样时残留局部条件；修正重叠设为 0 时被误替换为 128 的问题。

验证：新增 7 项 CPU 测试，覆盖分块权重覆盖、失败回滚、采样器恢复、时间分块帧序、第三方放大模型兼容、WebP 预览及 JPEG 回退。未进行真实 H3 GPU 分块画质、峰值显存或速度实测。重启 ComfyUI 并刷新浏览器后使用新增选项。

## 2026-09-09 — 段间引导保完整与句尾防裁切

合并上游截至 [`f1f68bc`](https://github.com/AIMixer/ComfyUI_MiniMaxH3_Director/commit/f1f68bc9fcc6ae3d965d89ea92fb1e1d8cdee13d) 的 5 个提交（含 1 个合并提交）。

### 主要变化

- 段间引导新增默认开启的「保完整」，保留模型帧数对齐产生的额外尾帧及对应音频，修复强制裁回界面时长导致的句尾吞字。最终拼接和一采缓存同步使用实际导出长度，避免后续再次裁掉保留的尾部。
- 代价是成片可能略长。例如 124 帧可见段加 22 帧引导时，采样对齐至 158 帧，去掉引导后保留 136 帧，比原先多 12 帧（24 FPS 下为 0.5 秒）；具体总时长还受段间衔接裁剪影响。关闭「保完整」恢复原裁切行为。
- 参考音频标签只保留解码成功的槽位，失败槽位不导致后续音色编号错位；保留文件引用的按需解码策略。同名音频替换后，文件时间及大小参与缓存校验。
- 修复 FFmpeg 分段导出先关闭 stdin 再 communicate 引发的 flush 错误，合入上游 Linux 空目录问题的修复。
- 保留本分支的 20GB 显存策略、文本编码器释放、延迟 VAE 解码、低内存分段导出保护及音画输出对应逻辑。

### 更新后怎么用

1. 重启 ComfyUI 并刷新浏览器，开启「段间引导」时保持「保完整」勾选。
2. 建议重新运行全部段落。该选项参与缓存指纹，旧裁切缓存不能冒充完整结果；已经导出的文件不会自动补回被裁掉的声音。
3. 本修复针对导出裁切，不保证解决模型本身未生成完整台词的所有情况。

### 验证范围

- 新增 CPU 回归覆盖尾帧与音频同步保留、关闭开关的旧行为、最终拼接、工作流默认值、缓存失效及参考音频槽位。
- 包含真实 FFmpeg 音视频写入与回读测试；尚未进行真实 H3 GPU 长视频生成及 Linux 环境实测，不宣称显存或速度获得额外提升。

## 2026-09-08 — 段间引导重绘、自定义帧率与导出兼容

合并上游截至 [`7de4a95`](https://github.com/AIMixer/ComfyUI_MiniMaxH3_Director/commit/7de4a95) 的 7 个提交。
本分支功能合并提交：[`01450ad`](https://github.com/Slash214/ComfyUI-MinMax-H3-Production-Director/commit/01450ad)。

### 主要变化

| 更新 | 解决的问题与使用效果 |
|---|---|
| 段间「引导＋重绘」 | 原有引导锁定上一段尾部；新模式允许衔接区域重新生成，为新段动作和场景变化提供更多空间。重绘幅度 0.40–0.95，默认 0.65；越大越允许变化。原有「引导」仍为默认，新模式不会在二采重复锁定前缀。 |
| 自定义帧率 | 修复部分路径仍按 24 FPS 计算生成帧数的问题。时长换算使用当前帧率，前后端统一按 MiniMax 的 17k+5 规则对齐，因此实际时长仍可能因对齐略有偏差。 |
| 官方条件节点兼容 | 将官方图生/参考视频条件节点改为关键字传参，兼容官方参数顺序调整，避免提示词与尺寸参数错位导致报错。 |
| 分段导出 | 已释放的旧段封面不再传给下游 CreateVideo / SaveVideo，避免误存静帧视频。完整视频仍保存在分段 MP4 文件中。 |

### 本分支的兼容处理

- 保留 `balanced_20gb`、文本编码器释放、延迟 VAE 解码和 RAM/VRAM 诊断。
- 「低内存分段导出」默认关闭；仅在分段导出且关闭一采确认时生效。成片及所需一采文件成功落盘、完成衔接后才允许释放；导出或裁剪重写失败时保留完整帧。
- 省略已释放段时，画面、一采画面、音频、原片按同一组段落选择输出，包含「选择运行」场景；`frame_count` 对应保留画面的实际帧数。
- 下游需要所有段的完整帧时，保持「低内存分段导出」关闭。开启后，应从 `output/minimax_seg_export/` 对应运行目录获取全部分段视频。
- 包元数据改为本仓库身份，不携带上游 Registry 自动发布工作流；本次更新通过 GitHub 仓库分发。

### 更新后怎么用

1. 更新插件后重启 ComfyUI，并刷新浏览器页面。
2. 使用新衔接方式：开启「段间引导」，选择「引导＋重绘」，可从默认幅度 0.65 开始比较效果。
3. 使用自定义帧率：修改帧率后检查各段时长和帧数，再提交生成。

### 验证范围

- 14 项 CPU 回归测试通过，覆盖内存释放保护、导出文件检查、段落输出对应、重绘遮罩及帧率换算。
- 前后端在 12、24、30、60 FPS 下的 5 秒帧数换算一致；Python 编译和 6 个修改的前端文件语法检查通过。
- 尚未完成真实 H3 GPU 工作流的画质、衔接效果和峰值内存对比。本次更新不宣称采样速度或画质已获得实测提升。
